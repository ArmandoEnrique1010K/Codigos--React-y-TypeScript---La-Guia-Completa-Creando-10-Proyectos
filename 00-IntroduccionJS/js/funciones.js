// Los módulos de ECMAScript son una forma de organizar y reutilizar el código de manera eficiente en aplicaciones de JavaScript, permitiendo dividir el código en archivos más pequeños y exportar e importar funciones, objetos, clases o variables entre ellos.

// Exportación Nombrada:
// Permite exportar múltiples funciones o valores desde un archivo.
export const sumar = (n1, n2) => n1 + n2;

export const restar = (n1, n2) => n1 - n2;

export const multiplicar = (n1, n2) => n1 * n2;

export const division = (n1, n2) => n1 / n2;
