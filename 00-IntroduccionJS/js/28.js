// Selectores
const heading = document.querySelector(".heading");

// console.log(heading); // Muestra el primer elemento con la clase "heading"
// console.log(heading.tagName); // Nombre de la etiqueta, por ejemplo "H1"
// console.log(heading.textContent); // Contenido de texto del elemento
// console.log(heading.classList); // Lista de clases del elemento
// console.log(heading.id); // El valor del atributo id, si existe

const enlaces = document.querySelector(".navegacion a");
const enlaces2 = document.querySelectorAll(".navegacion a");

console.log(enlaces2); // Muestra un NodeList con todos los enlaces dentro de la clase "navegacion"

// CONTINUA AQUI
