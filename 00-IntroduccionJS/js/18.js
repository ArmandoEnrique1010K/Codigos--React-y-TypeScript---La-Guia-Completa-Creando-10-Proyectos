// Condicionales

const disponible = 200;
const retirar = 200;

// Condicional if y else
if (disponible >= retirar) {
  // Se cumple la condición
  console.log("Si puedes retirar");
} else {
  // No se cumple la condición
  console.log("Saldo insuficiente");
}

// Operadores de comparación
// > Mayor que
// < Menor que
// >= Mayor o igual que
// <= Menor o igual que
// == igual (comparación de valor)
// === Igual estricto (compara valor y tipo)
// !== Diferente a (comparación de valor)
